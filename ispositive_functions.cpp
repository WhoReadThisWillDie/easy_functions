#include "easy.h"

bool itc_ispositive(int num1)
{
    if (num1 >= 0)
        return true;
    return false;
}

bool itc_ispositive_d(double num1)
{
    if (num1 >= 0)
        return true;
    return false;
}
